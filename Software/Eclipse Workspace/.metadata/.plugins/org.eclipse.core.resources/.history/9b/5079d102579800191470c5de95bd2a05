#include "./Drivers/Keyboard.h"


void keyboardTest()
{
	PS2_Device_t dev;
	int status = PS2_Init(PS21,&dev);

	if(status==0)
	{
		if(dev == PS2_KEYBOARD)
		{
			while(1)
			{
				uint8_t byte;
				if(PS2_ReadByte(PS21, &byte) == 0)
				{

				}
			}
		}
	}

}

